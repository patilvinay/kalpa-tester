const assert = require('assert');
const kalpaTester = require('../index.js');

describe('kalpaTester', () => {
  it('has a test', () => {
    assert(false, 'kalpaTester should have a test');
  });
});
